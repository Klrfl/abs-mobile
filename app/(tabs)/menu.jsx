import { Text, View } from "react-native";
// import useFetch from "../../components/useFetch";

export default function Menu() {
  // const {data, isLoading, error} = useFetch("menu")
  return (
    <View>
      <Text>See all available menus</Text>
    </View>
  )
}